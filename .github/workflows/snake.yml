name: Generate Snake Animation

on:
  # Run this workflow automatically on a schedule
  schedule:
    - cron: '0 6 * * *' # Runs every day at 6 AM UTC

  # Allow manual runs from the Actions tab
  workflow_dispatch:


permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate Snake SVG
        uses: Platane/snk@v3
        with:
          # Your GitHub username
          github_user_name: ${{ github.repository_owner }}

          # The output SVG file path
          outputs: |
            dist/github-contribution-grid-snake.svg?palette=github-light&color_snake=#3b82f6&color_dots=#bfdbfe,#93c5fd,#60a5fa,#3b82f6,#2563eb
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#60a5fa&color_dots=#1e3a8a,#1e40af,#2563eb,#3b82f6,#60a5fa

      - name: Push to repo
        run: |
            git config --global user.name "github-actions[bot]"
            git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add dist
            git commit -m "Generate Contribution Snake" || exit 0
            git pull
            git push